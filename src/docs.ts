export const apiDocs = 
`# Duolingo streak API

## GET /streak/{username}
Returns current streak of Duolingo user as just a number.

## GET /user/{username}
Returns simplified user data including current streak, XP, and courses.
`